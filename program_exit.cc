#include <iostream>
#include <thread>

int main() {
  // thread sleep 20 seconds
  //   std::this_thread::sleep_for(std::chrono::seconds(100));
  float a = -0.0 / -0.0;
  std::cout << a << std::endl;
  // int *p = nullptr;
  // p[2] = 2;
  return 0;
}
